## Internet
-(ICANN)- assigns top level domains and ip address

## Network performance metrics
```
throughput-is the rate of data transfer(a.k.a bandwidth)
usually measured in bytes per second

latency- is the delay of action. it is the time taken for a bit of data to arrive at destination
often measured in RTT(round trip time)time to get a response

```